{
  "name": "stadium-lights",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev:server": "npm run dev -w @stadium-lights/server",
    "dev:mobile": "npm run start -w @stadium-lights/mobile",
    "build:shared": "npm run build -w @stadium-lights/shared",
    "build:server": "npm run build -w @stadium-lights/server"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
