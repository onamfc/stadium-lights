PORT=3000
DATABASE_URL=postgresql://user:password@localhost:5432/stadium_lights

# CORS - comma-separated list of allowed origins, or * for all (dev only)
# Example: https://stadiumlights.io,https://go.stadiumlights.io
ALLOWED_ORIGINS=*

# Set to 'production' for production deployments
NODE_ENV=development
